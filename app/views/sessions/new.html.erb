<h1>Authenticate as one of the following users</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, session_url(user_id: user.id), method: :post %></li>
  <% end %>
</ul>

rooms

<ul>
  <% @rooms.each do |room| %>
    <li><%= link_to room.name, room_url(room) %></li>
  <% end %>
</ul>

<%= link_to 'ルーム作成', new_room_url %>
